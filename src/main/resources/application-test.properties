# Test profile - H2 Database Configuration
spring.application.name=scg-costmanager-test

#===============================
# H2 DATABASE CONFIGURATION
#===============================
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

# H2 Console (for debugging)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

#===============================
# JPA/HIBERNATE CONFIGURATION  
#===============================
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

#===============================
# FLYWAY CONFIGURATION
#===============================
# Disable Flyway for tests (use create-drop instead)
spring.flyway.enabled=false

#===============================
# LOGGING CONFIGURATION
#===============================
logging.level.root=WARN
logging.level.br.com.alg.scg=INFO
logging.level.org.springframework.web=WARN
logging.level.org.hibernate.SQL=WARN

# IMPORTANTE: Este profile é usado para TODOS os testes
# Comando: mvn test -Dspring.profiles.active=test